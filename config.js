/* ============================================================
   🔧 DEV ACADEMY · CONFIG (mesmo padrão do CORTEX)
   Cole aqui as credenciais do projeto Supabase da Academy.
   Project Settings → API → URL e anon public key.
   ⚠️ Projeto PRÓPRIO da Academy — nunca o do CORTEX.
   ============================================================ */
const SUPABASE_CONFIG = {
  url: '',      // ex.: 'https://abcdefgh.supabase.co'
  anonKey: ''   // ex.: 'eyJhbGciOi...'
};
